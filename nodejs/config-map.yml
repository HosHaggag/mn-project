kind: ConfigMap 
apiVersion: v1 
metadata:
  name: nodejs-configmap 
  namespace: default
data:
  # Configuration values can be set as key-value properties
  DBuser: admin
  DBpass: password
  yasser: hamada
  hossam: yasser
  DBhosts: mongod-0.mongodb-service,mongod-1.mongodb-service
  
